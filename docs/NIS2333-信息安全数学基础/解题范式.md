# 解题范式

- [Back to Course Home](index.md)

## 证明相等

两个集合 $A,B$ 相等：

1. 证明 $A \subseteq B$
2. 证明 $B \subseteq A$

两个数 $a,b$ 相等：

1. 证明 $a \mid b$
2. 证明 $b \mid a$

## 等价关系

若要证明 $\sim$ 是一个等价关系：

1. 证明 **反身性**
2. 证明 **对称性**
3. 证明 **传递性**

## 代数运算

若要证明集合 $A$ 中的运算 $\cdot$ 是一个代数运算：

1. 证明 **封闭性**
2. 证明 **唯一性** （通常显然）

## 群

若要证明集合 $A$ 关于运算 $\cdot$ 构成一个群：

1. 证明 $\cdot$ 是 **代数运算**
2. 证明 **结合律** （左右结合律）
3. 证明 **单位元** $e$ 存在（左右单位元）
4. 证明 **逆元** 存在（左右逆元）

## 子群

若要证明群 $G$ 的子集 $H$ 是 $G$ 的子群，即 $H < G$：

- 定义：
	1. $H$ 在群的运算下封闭
	2. $H$ 有单位元
	3. $H$ 中每个元素都有逆元
- 定理 1：
	1. 证明 $H$ 是 $G$ 的 **非空** 子集
	2. 对任意 $a,b \in H$，证明 $a b \in H$
	3. 对任意 $a \in H$，证明 $a^{-1} \in H$
- 定理 2：
	1. 证明 $H$ 是 $G$ 的 **非空** 子集
	2. 对任意 $a,b \in H$，证明 $a b^{-1} \in H$

## 正规子群

若要证明群 $G$ 的子群 $H$ 是 $G$ 的正规子群，即 $H \triangleleft G$，则下面几个条件等价

1. $H$ 是 $G$ 的正规子群
2. $\forall g \in G$，$g H = H g$
3. $\forall g \in G$，$g H g^{-1} = H$
4. $\forall g \in G$，$g H g^{-1} \subseteq H$
5. $\forall g \in G$，$h \in H$，则 $g h g^{-1} \in H$

## 群同态

若要证明两个群 $G$ 与 $G^{\prime}$ 同态：

1. 建立群 $G$ 与群 $G^{\prime}$ 的元素间的对应关系 $\phi$，并证明 $\phi$ 是 $G$ 到 $G^{\prime}$ 的 **映射**
	- 即对 $\forall x,y \in G$，证明由 $x=y$ 可推出 $\phi(x)=\phi(y)$
2. 证明 $\phi$ **保持运算**
	- 即对 $\forall x,y \in G$，证明 $\phi(x y)=\phi(x) \phi(y)$
3. 若还能证明 $\phi$ 是 $G$ 到 $G^{\prime}$ 的 **满射**，则 $\phi$ 是 $G$ 到 $G^{\prime}$ 的 **满同态映射**
4. 若还能证明 $\phi$ 是 $G$ 到 $G^{\prime}$ 的 **单射**，则 $\phi$ 是 $G$ 到 $G^{\prime}$ 的 **单同态映射**

## 群同构

若要证明两个群 $G$ 与 $G^{\prime}$ 同构：

1. 构造群 $G$ 与群 $G^{\prime}$ 的元素间的对应关系 $\phi$，并证明 $\phi$ 是 $G$ 到 $G^{\prime}$ 的 **映射**
	- 即对 $\forall x,y \in G$，证明由 $x=y$ 可推出 $\phi(x)=\phi(y)$
2. 证明 $\phi$ 是 $G$ 到 $G^{\prime}$ 的 **单射**
	- 即对 $\forall x,y \in G$，证明由 $\phi(x)=\phi(y)$ 可推出 $x=y$
3. 证明 $\phi$ 是 $G$ 到 $G^{\prime}$ 的 **满射**
	- 即对 $\forall x^{\prime} \in G^{\prime}$，证明存在（构造） $x \in G$，使 $\phi(x)=x^{\prime}$
4. 证明 $\phi$ **保持运算**
	- 即对 $\forall x,y \in G$，证明 $\phi(x y)=\phi(x) \phi(y)$

若要证明 $G / K \cong G^{\prime}$，即 $G / K$ 与 $G^{\prime}$ 同构：

1. 建立群 $G$ 与群 $G^{\prime}$ 的元素之间的对应关系 $\phi$，并证明 $\phi$ 为 $G$ 到 $G^{\prime}$ 的 **映射**
2. 证明 $\phi$ 为 $G$ 到 $G^{\prime}$ 的 **满射**
3. 证明 $\phi$ **保持运算**

    $$
    \phi(a b)=\phi(a) \phi(b)
    $$

4. 综上 $\phi$ 是 $G$ 到 $G^{\prime}$ 的 **满同态映射**
5. 计算同态的 **核** $\operatorname{Ker} \phi$，使得 $K=\operatorname{Ker} \phi$
6. 应用 **群同态基本定理** 得 $G / \operatorname{Ker} \phi \cong G^{\prime}$

## 环

若要证明 $R$ 是一个环：

1. 证明 **加法封闭**
	- $\forall x,y \in R$：$x+y\in R$
2. 证明加法满足 **加法结合律** 和 **加法交换律** 
	- $\forall x,y,z \in R$：$x+y=y+x$，$x+(y+z)=(x+y)+z$
3. 找到加法 **零元**
	- $\exists 0 \in R,\forall x \in R$：$x+0=x$
4. 找到加法 **负元**
	- $\forall x \in R,\exists -x \in R$：$x+(-x)=0$
5. 证明 **乘法封闭**
	- $\forall x,y \in R$：$x\cdot y\in R$
6. 证明乘法满足 **结合律**
	- $\forall x,y,z \in R$：$(x\cdot y)\cdot z = x\cdot (y \cdot z)$
7. 证明乘法对加法满足 **两个分配律**
	- $\forall x,y,z \in R

	    $$
	    $ \begin{array}{c} x\cdot (y + z) = x\cdot y + x\cdot z\\\ (y+z)\cdot x = y\cdot x + z\cdot x \end{array}
	    $$

8. 综上可得 $R$ 是一个环
9. 如果 $R$ 的乘法满足交换律，则 $R$ 是一个 **交换环**
10. 如果 $R$ 的乘法有单位元，则 $R$ 是一个有 **单位元** 的环

其中，1 - 4 即证明 $(R,+)$ 是一个 **加法交换群**，5 - 7 即证明 $R$ 还具有乘法代数运算。

## 整环

若要证明 $R$ 是一个整环：

1. 证明 $R$ 是一个 **交换环**
2. 证明 $R$ 中有 **单位元** 且单位元不为零元
3. 证明 $R$ 中没有 **零因子**
	- 即对 $\forall a,b \in R$，如果 $a\cdot b=0$，则 $a=0$ 或 $b=0$

## 域

若要证明 $F$ 是一个域：

1. 证明 $F$ 是一个 **交换环**
2. 证明 $F$ 中有 **单位元** 且单位元不为零元
3. 证明 $F$ 中每个 **非零元都可逆**
	- 即对 $\forall a \in F$ 且 $a \ne 0$，存在 $b \in F$ 使得 $a\cdot b=1$

## 子环

若要证明环 $R$ 的子集 $S$ 是 $R$ 的子环：

- 定理 1：
	1. 证明 $S$ 是 $R$ 的 **非空子集** （通常显然）
	2. 证明 $(S,+)$ 是 $(R,+)$ 的 **加法子群**
	3. $S$ 关于 $R$ 的 **乘法封闭** ，即对任意的 $a,b \in S$，有 $a b \in S$
- 定理 2：
	1. 证明 $S$ 是 $R$ 的 **非空子集** （通常显然）
	2. 证明 $\forall a,b \in S$，有 $a-b \in S$（**减法封闭**）
	3. 证明 $\forall a,b \in S$，有 $a b \in S$（**乘法封闭**）

## 理想

若要证明环 $R$ 的子集 $I$ 是 $R$ 的理想：

1. 证明 $I$ 是 $R$ 的 **非空子集** （通常显然）
2. 证明 $\forall r_{1},r_{2} \in I$，$r_{1}-r_{2} \in I$（**减法封闭**）
3. 证明 $\forall r \in I$，$s \in R$，$r s,s r \in I$（**乘法吸收**）

## 环同态

若要证明 $\phi : R \rightarrow R^{\prime}$ 是一个同态映射：

1. 证明 $\phi$ 为 $R$ 到 $R^{\prime}$ 的 **映射**
	- 即对 $\forall a,b \in R$，证明由 $a=b$ 可推出 $\phi(a)=\phi(b)$
2. 证明 $\phi$ **保持运算**
	- 即对 $\forall a,b \in R$，证明
		1. $\phi(a+b)=\phi(a)+\phi(b)$（**加法保持**）
		2. $\phi(a b)=\phi(a) \phi(b)$（**乘法保持**）
3. 若还能证明 $\phi$ 是 $R$ 到 $R^{\prime}$ 的 **满射**，则 $\phi$ 是 $R$ 到 $R^{\prime}$ 的 **满同态映射**
4. 若还能证明 $\phi$ 是 $R$ 到 $R^{\prime}$ 的 **单射**，则 $\phi$ 是 $R$ 到 $R^{\prime}$ 的 **单同态映射**

## 环同构

若要证明环 $R$ 与环 $R^{\prime}$ 同构：

1. 构造环 $R$ 与环 $R^{\prime}$ 的元素间的对应关系 $\phi$，并证明 $\phi$ 是 $R$ 到 $R^{\prime}$ 的 **映射**
	- 即对 $\forall a,b \in R$，证明由 $a=b$ 可推出 $\phi(a)=\phi(b)$
2. 证明 $\phi$ 是 $R$ 到 $R^{\prime}$ 的 **单射**
3. 证明 $\phi$ 是 $R$ 到 $R^{\prime}$ 的 **满射**
4. 证明 $\phi$ **保持运算**

若要证明 $R / I \cong R^{\prime}$，即 $R / I$ 与 $R^{\prime}$ 同构：

1. 建立环 $R$ 与环 $R^{\prime}$ 的元素之间的对应关系 $\phi$，并证明 $\phi$ 为 $R$ 到 $R^{\prime}$ 的 **映射**
2. 证明 $\phi$ 为 $R$ 到 $R^{\prime}$ 的 **满射**
3. 证明 $\phi$ **保持运算**

	$$
	\phi(a+b)=\phi(a)+\phi(b),\quad \phi(a b)=\phi(a) \phi(b)
	$$

4. 综上 $\phi$ 是 $R$ 到 $R^{\prime}$ 的 **满同态映射**
5. 计算同态的 **核** $\operatorname{Ker} \phi$，使得 $I=\operatorname{Ker} \phi$
6. 应用 **环同态基本定理** 得 $R / \operatorname{Ker} \phi \cong R^{\prime}$

## 素理想
若要证明交换环 $R$ 的理想 $P$ 是素理想：

1. 证明 $P$ 是 $R$ 的 **真理想**
2. 证明对任意 $a,b \in R$，如果 $a b \in P$，则 $a \in P$ 或 $b \in P$

## 极大理想

若要证明交换环 $R$ 的理想 $I$ 是极大理想：

1. 证明 $I$ 是 $R$ 的 **真理想**
2. 设 $J$ 是 $R$ 的任意理想，且 $I \subsetneq J \subseteq R$（左右夹击）
3. 任取 $x \in J$ 且 $x \notin I$
4. 根据 $x \notin I$，获取约束条件，如不整除、互素等
5. 构造 $1 = \cdots \in J$
6. 因此 $J=R$
7. 故 $I$ 是 $R$ 的极大理想

若要证明交换环 $R$ 的理想 $I$ 是唯一极大理想：

1. 证明 $I$ 是 $R$ 的 **真理想**
2. 设 $J$ 是 $R$ 的任意 **不包含于** $I$ 的理想
3. 任取 $x \in J$ 且 $x \notin I$
4. 根据 $x \notin I$，获取约束条件，如不整除、互素等
5. 构造 $1 = \cdots \in J$
6. 因此 $J=R$
7. 故 $R$ 的所有真理想都包含于 $I$，即 $I$ 是唯一极大理想

## 特征

若要证明 $R$ 的特征为 $n$，$n \ne 0$：

- 方法一：
	1. 给出正整数 $n$，使得 $\forall a \in R, na=0$（存在 $n$）
	2. 证明 $\forall k, 1\le k <n, \exists b \in R, kb\ne 0$（没有比 $n$ 更小的）
- 方法二：（若 $R$ 有单位元）
	1. 找出 $R$ 的单位元 $e$
	2. 计算 $e$ 关于加法的阶 $n$
	3. 特征 $\operatorname{Char} R = n$

若要证明 $R$ 的特征为 $n=0$：

- 证明 $R$ 的单位元 $e$ 关于加法的阶为无穷大